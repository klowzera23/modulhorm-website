# ModulHorm

Proyecto web con frontend estático y backend Express + SQLite para almacenar contactos del formulario.

## Ejecutar

1. npm install
2. npm start

La app queda disponible en http://localhost:3000

## Endpoints

- GET /health
- POST /api/contact
- GET /api/contacts
